{
  "name": "@rambleraptor/homestead-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "cf:preview": "wrangler pages dev",
    "cf:deploy": "npm run build && wrangler pages deploy"
  },
  "dependencies": {
    "vitepress": "^1.6.4"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "wrangler": "^4.98.0"
  }
}
