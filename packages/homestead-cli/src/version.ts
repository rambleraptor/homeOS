/**
 * The homestead release line this launcher belongs to. Kept as a literal
 * because the compiled binary has no package.json to read at runtime; bump it
 * here when cutting a release (scaffolded projects and backup manifests both
 * derive from it).
 */
export const HOMESTEAD_VERSION = '0.2.0';
