name: Code Quality & Security

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write
  id-token: write

jobs:
  claude-code-review:
    runs-on: ubuntu-latest
    name: Claude AI Code Review
    if: github.event.issue.pull_request && contains(github.event.comment.body, '/claude')
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write

    steps:
      - name: Get PR details
        id: pr
        uses: actions/github-script@v7
        with:
          script: |
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });
            core.setOutput('ref', pr.data.head.ref);
            core.setOutput('sha', pr.data.head.sha);
            core.setOutput('number', context.issue.number);

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.pr.outputs.sha }}
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ steps.pr.outputs.number }}

            This is a modular Homestead application built with:
            - React 19 + TypeScript + Next.js
            - aepbase backend (AEP-compliant dynamic REST)
            - App Registry pattern for extensibility
            - Role-Based Access Control (RBAC)

            Please review this pull request and provide feedback on:

            **Code Quality & Architecture:**
            - Adherence to modular architecture pattern (apps should be self-contained)
            - TypeScript type safety and best practices
            - React 19 patterns and hooks usage (no unused React imports needed)
            - Component composition and reusability

            **Security:**
            - Authentication and authorization implementation
            - RBAC permission checks (usePermissions, PermissionGuard)
            - aepbase resource access scoping and user-parent isolation
            - XSS, injection, and other OWASP vulnerabilities
            - Sensitive data handling

            **App System:**
            - New apps follow the established pattern in src/apps/
            - App registration in src/apps/registry.ts
            - Proper route definitions and permissions
            - App-specific types are well-defined

            **Performance & Best Practices:**
            - Efficient React Query usage and caching
            - Proper error handling
            - Loading states and user feedback
            - Accessibility considerations

            **Testing & Documentation:**
            - Code clarity and maintainability
            - Inline documentation where needed
            - Component prop types

            Be constructive and specific. Highlight both strengths and areas for improvement.
            Use the repository's documentation for context on architecture decisions.

            Use `gh pr comment` with your Bash tool to leave your review as a comment on the PR.

          claude_args: '--allowed-tools "Bash(gh issue view:*),Bash(gh search:*),Bash(gh issue list:*),Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh pr list:*)"'
  lint-and-typecheck:
    runs-on: ubuntu-latest
    name: Lint & Type Check
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: make install

      - name: Run TypeScript type check
        run: make type-check

      - name: Run ESLint
        run: make lint

  build-test:
    runs-on: ubuntu-latest
    name: Build and Test
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: make install

      - name: Build application
        run: make build

  test:
    runs-on: ubuntu-latest
    name: Run Tests
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: make install

      # `make test` runs the frontend (vitest) and CLI/server (bun test)
      # suites, so this job needs Node + Bun.
      - name: Run tests
        run: make test

  security-scan:
    runs-on: ubuntu-latest
    name: Security Scan
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: make install

      - name: Run npm audit
        run: make audit || true

      - name: Check for secrets
        run: |
          echo "Checking for exposed secrets..."
          ! grep -r -i "api_key\|secret\|password\|token" frontend/src --include="*.ts" --include="*.tsx" | grep -v "placeholder" | grep -v "example" || echo "Warning: Potential secrets found in code"

  e2e-tests:
    runs-on: ubuntu-latest
    name: E2E Tests
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install workspace dependencies
        run: make install

      - name: Install e2e test dependencies
        run: cd tests/e2e && npm install

      - name: Install Playwright browsers
        run: cd tests/e2e && npx playwright install --with-deps chromium

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Run E2E tests
        run: cd tests/e2e && npm test
